name = "open-ai-api"
version = "0.1.0"
language = "rust"
type = "component"

[build]
command = "cargo build --target wasm32-wasip2 --release"

[component]
wit_world = "main"
wasm_target = "wasm32-wasip2"
